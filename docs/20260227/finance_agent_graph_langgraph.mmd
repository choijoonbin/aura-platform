---
config:
  flowchart:
    curve: linear
---
graph TD;
	__start__([<p>__start__</p>]):::first
	analyze(analyze)
	evidence_gather(evidence_gather)
	plan(plan)
	execute(execute)
	tools(tools)
	reflect(reflect)
	__end__([<p>__end__</p>]):::last
	__start__ --> analyze;
	analyze -. &nbsp;gather&nbsp; .-> evidence_gather;
	analyze -.-> plan;
	evidence_gather --> plan;
	execute -.-> reflect;
	execute -. &nbsp;use_tools&nbsp; .-> tools;
	plan --> execute;
	tools --> reflect;
	reflect --> __end__;
	classDef default fill:#f2f0ff,line-height:1.2
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc
